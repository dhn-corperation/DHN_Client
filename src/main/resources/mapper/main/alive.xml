<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dhn.client.alive.mapper.SendRequest">

    <select id="select_alive_count" parameterType = "com.dhn.client.bean.SQLParameter" resultType = "int">
        select count(1) as cnt
        from ${alive_table}
        where role_type = '${role_type}'
    </select>

    <insert id="alive_insert_data" parameterType = "com.dhn.client.bean.SQLParameter">
        insert into ${alive_table}(role, updated_date, role_type) values('${role}', sysdate, '${role_type}')
    </insert>

    <resultMap id="RequestData" type="com.dhn.client.bean.AliveData">
        <result column="ROLE" property="role"/>
        <result column="FLAG" property="flag"/>
    </resultMap>

    <select id="select_alive_data" parameterType="com.dhn.client.bean.SQLParameter" resultMap = "RequestData">
        SELECT
            ROLE,
            CASE
                WHEN UPDATED_DATE &lt;= SYSDATE-(60/24/60/60) THEN 'Y'
                ELSE 'N'
                END AS FLAG
        FROM ${alive_table}
        where role_type = '${role_type}'
    </select>

    <update id="alive_update_date" parameterType="com.dhn.client.bean.SQLParameter">
        update ${alive_table}
        set updated_date = sysdate
        where role_type = '${role_type}'
    </update>

    <update id="alive_update_agent" parameterType="com.dhn.client.bean.SQLParameter">
        update ${alive_table}
        set updated_date = sysdate,
            role = '${role}'
        where role_type = '${role_type}'
    </update>

    <select id="select_alive_count_mysql" parameterType = "com.dhn.client.bean.SQLParameter" resultType = "int">
        select count(1) as cnt
        from ${alive_table}
        where role_type = '${role_type}'
    </select>

    <insert id="alive_insert_data_mysql" parameterType = "com.dhn.client.bean.SQLParameter">
        insert into ${alive_table}(role, updated_date, role_type) values('${role}', now(), '${role_type}')
    </insert>

    <resultMap id="RequestData_mysql" type="com.dhn.client.bean.AliveData">
        <result column="ROLE" property="role"/>
        <result column="FLAG" property="flag"/>
    </resultMap>

    <select id="select_alive_data_mysql" parameterType="com.dhn.client.bean.SQLParameter" resultMap = "RequestData">
        SELECT
            ROLE,
            CASE
                WHEN UPDATED_DATE &lt;= DATE_SUB(NOW(), INTERVAL 1 MINUTE) THEN 'Y'
                ELSE 'N'
                END AS FLAG
        FROM ${alive_table}
        where role_type = '${role_type}'
    </select>

    <update id="alive_update_date_mysql" parameterType="com.dhn.client.bean.SQLParameter">
        update ${alive_table}
        set updated_date = now()
        where role_type = '${role_type}'
    </update>

    <update id="alive_update_agent_mysql" parameterType="com.dhn.client.bean.SQLParameter">
        update ${alive_table}
        set updated_date = now(),
            role = '${role}'
        where role_type = '${role_type}'
    </update>

</mapper>