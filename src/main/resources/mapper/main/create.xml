<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dhn.client.create.mapper.SendRequest">
    <select id="logTableCheck" parameterType="com.dhn.client.bean.SQLParameter" resultType="int">
        SELECT COUNT(1)
        FROM ALL_TABLES
        WHERE TABLE_NAME = UPPER(#{logTable})
    </select>

    <update id="createLogTable" parameterType="map">
        CREATE TABLE "${logTable}" AS
        SELECT * FROM "${msgTable}" WHERE 1=0
    </update>

    <select id="tableCheck" parameterType="com.dhn.client.bean.SQLParameter" resultType="int">
        SELECT COUNT(1)
        FROM ALL_TABLES
        WHERE TABLE_NAME = UPPER(#{msg_table})
    </select>

    <select id="aliveTableCheck" parameterType="com.dhn.client.bean.SQLParameter" resultType="int">
        SELECT COUNT(1)
        FROM ALL_TABLES
        WHERE TABLE_NAME = UPPER(#{alive_table})
    </select>

    <update id="createAliveTable" parameterType="com.dhn.client.bean.SQLParameter">
        CREATE TABLE "${alive_table}"
        (	"ROLE" VARCHAR2(10),
             "UPDATED_DATE" TIMESTAMP (6),
             "ROLE_TYPE" VARCHAR2(5)
        )
    </update>

    <update id="createTable" parameterType="com.dhn.client.bean.SQLParameter">
        CREATE TABLE "${msg_table}"
        (	"SNDNG_PRNMNT_YMD" CHAR(8),
             "MSG_MST_NO" NUMBER(22,0) NOT NULL ENABLE,
             "GDBK_SNDNG_PRNMNT_TM" CHAR(6),
             "MSG_TMPLT_CD" VARCHAR2(30),
             "ISND_DMND_YMD" CHAR(8) DEFAULT TO_CHAR(SYSDATE, 'YYYYMMDD'),
             "SNDNG_TYPE_CD" CHAR(2),
             "SNDPTY_ID" VARCHAR2(100),
             "SNDPTY_NM" VARCHAR2(100),
             "MOBL_BROF_CD" VARCHAR2(8),
             "CUST_NM" VARCHAR2(100),
             "SNDPTY_TELNO" VARCHAR2(20),
             "RCVR_TELNO" VARCHAR2(20),
             "MSG_TTL" VARCHAR2(50),
             "MSG_CN" VARCHAR2(4000),
             "SNDNG_PFR_RNKG" NUMBER(2,0) DEFAULT 1,
             "MOBL_REG_TYPE_CD" CHAR(2),
             "GDBK_TRSM_MTHD_CD" VARCHAR2(2) DEFAULT '01',
             "MSG_STTS_CD" VARCHAR2(8) DEFAULT '00',
             "ISND_PRCS_DT" DATE,
             "GDBK_SNDNG_DMND_TMST" TIMESTAMP(6),
             "MSG_SNRS_CD" VARCHAR2(8),
             "GDBK_SNRS_MSG_CN" VARCHAR2(200),
             "MOBL_SYS_CD" VARCHAR2(10),
             "SPAM_YN" CHAR(1) DEFAULT 'N',
             "BTN_NM" VARCHAR2(500),
             "BTN_KIND_NM" VARCHAR2(30),
             "URL_MOBL_ANDR_CN" VARCHAR2(1000),
             "URL_PC_IOS_CN" VARCHAR2(1000),
             "CUST_INTG_CONT_NO" VARCHAR2(16),
             "LINK_SNDNG_TYPE_CD" VARCHAR2(10),
             "LINK_SNDNG_MST_NO" NUMBER(22,0),
             "LINK_FRST_SNDNG_TYPE_CD" CHAR(2),
             "GDBK_LAST_SNDNG_TYPE_CD" CHAR(2),
             "LINK_LAST_SNDNG_SCS_YN" CHAR(1),
             "MDFCN_DT" DATE DEFAULT SYSTIMESTAMP,
             "MDFR_ID" VARCHAR2(100),
             "INTENAL_MESSAGE" VARCHAR2(4000)
        )
    </update>

    <update id="createPrimaryKey" parameterType="com.dhn.client.bean.SQLParameter">
        ALTER TABLE "${msg_table}" ADD CONSTRAINT "${msg_table}_MST_NO_PK" PRIMARY KEY("MSG_MST_NO")
    </update>

    <update id="createIndex1" parameterType="com.dhn.client.bean.SQLParameter">
        CREATE INDEX "${msg_table}_SELECT_IDX01" ON "${msg_table}" ("SEND_TYPE_CD", "MESSAGE_STATUS_CD", "REG_TYPE_CD", "ETRS_MTCD", "SEND_EXPECTED_DT", "SEND_EXPECTED_TIME")
    </update>

    <update id="createIndex2" parameterType="com.dhn.client.bean.SQLParameter">
        CREATE INDEX "${msg_table}_SELECT_IDX02" ON "${msg_table}" ("INTENAL_MESSAGE")
    </update>

</mapper>