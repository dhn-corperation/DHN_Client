<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dhn.client.kakao.mapper.SendRequest">
	<select id="req_kao_count" parameterType = "com.dhn.client.bean.SQLParameter" resultType = "int">
		select count(1) as cnt
		  from ${msg_table}
		 where msg_type = '6'
		   and nvl(REQUEST_TIME, sysdate) &lt;= sysdate
		   and SEND_TIME is null
		   and STAT = '0'
		   and rownum = 1		 
	</select>
	<update id="req_kao_group_update" parameterType = "com.dhn.client.bean.SQLParameter" >
		update ${msg_table}
		   set STAT = '1'
		      ,SEND_TIME = to_date('${group_no}', 'yyyymmddhh24miss')
		 where MSEQ in (select r.MSEQ
		                    from ${msg_table} r
		                   where msg_type = '6'
		                     and nvl(REQUEST_TIME, sysdate) &lt;= sysdate
		                     and SEND_TIME is null
		                     and STAT = '0'
		                     and rownum &lt;= 1000)
	</update>

	<select id="req_kao_count_z" parameterType = "com.dhn.client.bean.SQLParameter" resultType = "int">
		select count(1) as cnt
		  from ${msg_table}
		 where msg_type = '6'
		   and nvl(REQUEST_TIME, sysdate) &lt;= sysdate
		   and SEND_TIME is null
		   and STAT = '0'
 		   and exists (SELECT 1 
 		          FROM ${zbsysmcd_table}
                  WHERE cdid='kkoagent' 
                    AND TO_CHAR(SYSDATE, 'YYYYMMDD') BETWEEN valifromdd AND valitodd
                    and cdval = 'DH')		   
		   and rownum = 1		 
	</select>
	<update id="req_kao_group_update_z" parameterType = "com.dhn.client.bean.SQLParameter" >
		update ${msg_table}
		   set STAT = '1'
		      ,SEND_TIME = to_date('${group_no}', 'yyyymmddhh24miss')
		 where MSEQ in (select r.MSEQ
		                    from ${msg_table} r
		                   where msg_type = '6'
		                     and nvl(REQUEST_TIME, sysdate) &lt;= sysdate
		                     and SEND_TIME is null
		                     and STAT = '0'
					 	     and exists (SELECT 1 
						 		           FROM ${zbsysmcd_table}
						                  WHERE cdid='kkoagent' 
						                    AND TO_CHAR(SYSDATE, 'YYYYMMDD') BETWEEN valifromdd AND valitodd
						                    and cdval = 'DH')		   
		                     and rownum &lt;= 1000)
	</update>
  
	<update id="req_sent_complete" parameterType = "com.dhn.client.bean.SQLParameter" >
		update ${msg_table} set STAT = '2'  where STAT = '1' and SEND_TIME = to_date('${group_no}', 'yyyymmddhh24miss') and msg_type = '6'
	</update>

	<update id="req_sent_init" parameterType = "com.dhn.client.bean.SQLParameter" >
		update ${msg_table} set STAT = '0', SEND_TIME = null where STAT = '1' and SEND_TIME = to_date('${group_no}', 'yyyymmddhh24miss') and msg_type = '6'		
	</update>
	
	<resultMap id="RequestTable" type="com.dhn.client.bean.KAORequestBean">
		<result column="msg_seq" property="msgid"/>
		<result column="messagetype" property="messagetype"/>
		<result column="msg_txt" property="msg"/>
		<result column="sms_txt" property="msgsms"/>
		<result column="call_to" property="phn"/>
		<result column="call_from" property="smssender"/>
		<result column="pcom" property="pcom"/>
		<result column="userdata" property="pinvoice"/>
		<result column="remark3" property="remark3"/>
		<result column="msg_txt_title" property="smslmstit"/>
		<result column="crypto" property="crypto"/>
		<result column="curr_date" property="regdt"/>
		<result column="btn_1" property="button1"/>
		<result column="btn_2" property="button2"/>
		<result column="btn_3" property="button3"/>
		<result column="btn_4" property="button4"/>
		<result column="btn_5" property="button5"/>
		<result column="template_cd" property="tmplid"/>
		<result column="profile_key" property="profile"/> 
		<result column="snd_dttm" property="reservedt"/>
	</resultMap>
			
	<select id="req_kao_select" parameterType = "com.dhn.client.bean.SQLParameter" resultMap = "RequestTable">
		select 'K' || MSEQ as msg_seq
		      ,'AT' as messagetype
		      ,TEXT as msg_txt
		      ,null as sms_txt
		      ,(case when DSTADDR like '82%' then 
		      		DSTADDR
		      		 when DSTADDR like '010%' then
		      		'82' || substr(DSTADDR, 2, 15)  
		        else 
		            '82' || substr(DSTADDR, 2, 15)  
		        end)  as call_to
		      ,CALLBACK as call_from
	          ,K_NEXT_TYPE  as remark3
		      ,SUBJECT as msg_txt_title
		      ,'D' as pcom
		      ,'N' as crypto
		      ,to_char(sysdate, 'yyyy-mm-dd hh24:mi:ss') as curr_date
              ,(case when k_button_type is not null then 
              		'{"type":"' || decode(trim(K_BUTTON_TYPE) ,'1','DS','2','WL','3','AL','4','BK', '5','MD')||'","name":"'||K_BUTTON_NAME ||'","url_mobile":"'||K_BUTTON_URL ||'","url_pc":"'||K_BUTTON_URL2 ||'"}' 
             	end) as btn_1 
              ,(case when k_button2_type is not null then 
              		'{"type":"' || decode(trim(K_BUTTON2_TYPE),'1','DS','2','WL','3','AL','4','BK', '5','MD')||'","name":"'||K_BUTTON2_NAME||'","url_mobile":"'||K_BUTTON2_URL||'","url_pc":"'||K_BUTTON2_URL2||'"}' 
              	end) as btn_2 
              ,(case when k_button3_type is not null then 
              		'{"type":"' || decode(trim(K_BUTTON3_TYPE),'1','DS','2','WL','3','AL','4','BK', '5','MD')||'","name":"'||K_BUTTON3_NAME||'","url_mobile":"'||K_BUTTON3_URL||'","url_pc":"'||K_BUTTON3_URL2||'"}' 
              	end) as btn_3 
              ,(case when k_button4_type is not null then 
              		'{"type":"' || decode(trim(K_BUTTON4_TYPE),'1','DS','2','WL','3','AL','4','BK', '5','MD')||'","name":"'||K_BUTTON4_NAME||'","url_mobile":"'||K_BUTTON4_URL||'","url_pc":"'||K_BUTTON4_URL2||'"}' 
              	end) as btn_4 
              ,(case when k_button5_type is not null then 
              		'{"type":"' || decode(trim(K_BUTTON5_TYPE),'1','DS','2','WL','3','AL','4','BK', '5','MD')||'","name":"'||K_BUTTON5_NAME||'","url_mobile":"'||K_BUTTON5_URL||'","url_pc":"'||K_BUTTON5_URL2||'"}' 
              	end) as btn_5 
		      ,K_TEMPLATE_CODE as template_cd
		      ,(case when SENDER_KEY = '7df81cea0e9489d472fc9e7f687eda4ad44da978' then 
		      			'afcf00bc7aca6b9415c1992ba020e5b733658975'
		             when SENDER_KEY = '8635452e038277f4d11d8b3b4513f53abb87a770' then 
		             	'089ff9aec0e0803e28e931b929c3a3a2d0b0173f'
		             else 
		                SENDER_KEY
		        end) as profile_key		
		      ,nvl(to_char(REQUEST_TIME,'yyyymmddhh24miss'), '00000000000000') as snd_dttm      
		  from ${msg_table} 
		 where STAT = '1'
		   and SEND_TIME = to_date('${group_no}', 'yyyymmddhh24miss')   
		   and msg_type = '6'
	</select>

	<insert id="result_log_insert1" parameterType="com.dhn.client.bean.Msg_Log">
		update ${table} 
		   set STAT = '${stat}'
		      ,RESULT = '${result}' 
		      ,REPORT_TIME = to_date('${report_time}', 'yyyy-mm-dd hh24:mi:ss')
		      ,TELECOM = '${telecom}' 
		      ,TCPRECV_TIME =  sysdate 
		      ,save_time = sysdate
		 where MSEQ = '${mseq}'
	</insert>

	<insert id="result_log_insert2" parameterType="com.dhn.client.bean.Msg_Log">
		insert into ${log_table} select * from ${table} where MSEQ = '${mseq}'
	</insert>	

	<insert id="result_log_insert3" parameterType="com.dhn.client.bean.Msg_Log">
		delete from ${table} a where MSEQ = '${mseq}'
	</insert>
	
</mapper>  