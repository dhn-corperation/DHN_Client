<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dhn.client.result.mapper.SendRequest">

    <select id="select_2nd_flag" parameterType="com.dhn.client.bean.Msg_Log" resultType="String">
        SELECT
            CASE
                WHEN LENGTH(trim(LINK_SEND_TYPE_CD)) > 0 AND TRIM(LINK_SEND_TYPE_CD) IS NOT NULL THEN 'Y'
                ELSE 'N'
                END AS flag
        FROM ${msg_table}
        WHERE MESSAGE_MST_NO = '${msgid}'
    </select>

    <update id="send_2nd_update" parameterType="com.dhn.client.bean.Msg_Log">
        update ${main_table}
            set SEND_RESULT_DT = SYSTIMESTAMP,
                MESSAGE_STATUS_CD = '02',
                SEND_RESULT_CD = '${result_code}',
                SEND_RESULT_MESSAGE = #{result_msg},
                MOD_DT = SYSTIMESTAMP,
                MOD_ID = #{mod_id},
                SEND_TYPE_CD = '${msg_type}'
        where MESSAGE_MST_NO = '${msgid}'
    </update>

    <update id="dhn_log_update" parameterType="com.dhn.client.bean.Msg_Log">
        update ${msg_table}
        set SEND_RESULT_DT = SYSTIMESTAMP,
            MESSAGE_STATUS_CD = '03',
            SEND_RESULT_CD = '${result_code}',
            SEND_RESULT_MESSAGE = #{result_msg},
            MOD_DT = SYSTIMESTAMP,
            MOD_ID = #{mod_id},
            SEND_TYPE_CD = '${msg_type}'
        where MESSAGE_MST_NO = '${msgid}'
    </update>

    <insert id="dhn_log_insert" parameterType="com.dhn.client.bean.Msg_Log">
        INSERT INTO ${log_date_table}
        SELECT * FROM ${msg_table}
        WHERE MESSAGE_MST_NO = '${msgid}'
    </insert>

    <delete id="dhn_log_delete" parameterType="com.dhn.client.bean.Msg_Log">
        delete from ${msg_table}
        where MESSAGE_MST_NO = '${msgid}'
    </delete>

    <update id="log_update" parameterType="com.dhn.client.bean.Msg_Log">
        update ${main_table}
            set SEND_RESULT_DT = SYSTIMESTAMP,
                SEND_RESULT_CD = '${result_code}',
                SEND_RESULT_MESSAGE = #{result_msg},
                MOD_DT = SYSTIMESTAMP,
                MOD_ID = #{mod_id},
                SEND_TYPE_CD = '${msg_type}'
        where MESSAGE_MST_NO = '${msgid}'
    </update>

    <delete id="log_delete" parameterType="com.dhn.client.bean.Msg_Log">
        delete from ${main_table}
        where MESSAGE_MST_NO = '${msgid}'
    </delete>

    <update id="phn_err_log_update" parameterType="com.dhn.client.bean.Msg_Log">
        update ${main_table}
        set SEND_RESULT_DT = SYSTIMESTAMP,
            MESSAGE_STATUS_CD = '91',
            SEND_RESULT_CD = '${result_code}',
            SEND_RESULT_MESSAGE = #{result_msg},
            MOD_DT = SYSTIMESTAMP,
            MOD_ID = #{mod_id}
        where MESSAGE_MST_NO in (${msgid})
    </update>

    <update id="phn_err_dhn_log_update" parameterType="com.dhn.client.bean.Msg_Log">
        update ${msg_table}
        set SEND_RESULT_DT = SYSTIMESTAMP,
            MESSAGE_STATUS_CD = '91',
            SEND_RESULT_CD = '${result_code}',
            SEND_RESULT_MESSAGE = #{result_msg},
            MOD_DT = SYSTIMESTAMP,
            MOD_ID = #{mod_id}
        where MESSAGE_MST_NO in (${msgid})
    </update>

    <insert id="phn_err_dhn_log_insert" parameterType="com.dhn.client.bean.Msg_Log">
        INSERT INTO ${log_date_table}
        SELECT * FROM ${msg_table}
        WHERE MESSAGE_MST_NO in (${msgid})
    </insert>

    <delete id="phn_err_dhn_log_delete" parameterType="com.dhn.client.bean.Msg_Log">
        delete from ${msg_table}
        where MESSAGE_MST_NO in (${msgid})
    </delete>

    <delete id="phn_err_log_delete" parameterType="com.dhn.client.bean.Msg_Log">
        delete from ${main_table}
        where MESSAGE_MST_NO in (${msgid})
    </delete>

    <update id="source_err_log_update" parameterType="com.dhn.client.bean.Msg_Log">
        update ${msg_table}
        set MESSAGE_STATUS_CD = '99',
            INTENAL_MESSAGE = #{source_err_msg},
            MOD_DT = SYSTIMESTAMP,
            MOD_ID = #{mod_id}
        where MESSAGE_MST_NO = '${msgid}'
    </update>
</mapper>