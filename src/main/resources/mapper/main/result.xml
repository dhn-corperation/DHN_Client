<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dhn.client.result.mapper.SendRequest">
    <update id="log_update" parameterType="com.dhn.client.bean.Msg_Log">
        update ${msg_table}
            set RSLTDTTM = '${real_send_date}',
                REPDTTM = SYSDATE,
                STATUS = '103',
                TELCODE = '${tel_code}',
                TYPE_FINAL = '${send_type}'
        where SEQNO = '${msgid}'
    </update>

</mapper>