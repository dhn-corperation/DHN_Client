<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dhn.client.nkakao.mapper.SendRequest">

	<select id="req_sms_count" parameterType = "com.dhn.client.bean.SQLParameter" resultType = "int">
		select count(1) as cnt
		from ${msg_table}
		where MSG_SNDNG_STTS_CD = '0'
		  and NVL(RSVT_DT,SYSDATE) &lt;= SYSDATE
		  and SNDNG_DT is null
		  and MSG_DCD = '${msg_type}'
		  and GROUP_NO is NULL
	</select>

	<update id="req_sms_group_update" parameterType = "com.dhn.client.bean.SQLParameter" >
		update ${msg_table}
		set MSG_SNDNG_STTS_CD = '1',
			GROUP_NO = '${group_no}'
		where DAT_ID in (
			select DAT_ID
			from ${msg_table}
			where MSG_SNDNG_STTS_CD = '0'
			  and NVL(RSVT_DT,SYSDATE) &lt;= SYSDATE
		  and SNDNG_DT is null
		  and MSG_DCD = '${msg_type}'
		  and GROUP_NO is null
			fetch first 1000 rows only
			)
	</update>

	<select id="req_lms_count" parameterType = "com.dhn.client.bean.SQLParameter" resultType = "int">
		select count(1) as cnt
		from ${msg_table}
		where MSG_SNDNG_STTS_CD = '0'
		  and NVL(RSVT_DT,SYSDATE) &lt;= SYSDATE
		  and SNDNG_DT is null
		  and MSG_DCD = '${msg_type}'
		  and GROUP_NO is NULL
	</select>

	<update id="req_lms_group_update" parameterType = "com.dhn.client.bean.SQLParameter" >
		update ${msg_table}
		set MSG_SNDNG_STTS_CD = '1',
			GROUP_NO = '${group_no}'
		where DAT_ID in (
			select DAT_ID
			from ${msg_table}
			where MSG_SNDNG_STTS_CD = '0'
			  and NVL(RSVT_DT,SYSDATE) &lt;= SYSDATE
		  and SNDNG_DT is null
		  and MSG_DCD = '${msg_type}'
		  and GROUP_NO is null
			fetch first 1000 rows only
			)
	</update>

	<select id="req_mms_count" parameterType = "com.dhn.client.bean.SQLParameter" resultType = "int">
		select count(1) as cnt
		from ${msg_table}
		where MSG_SNDNG_STTS_CD = '0'
		  and NVL(RSVT_DT,SYSDATE) &lt;= SYSDATE
		  and SNDNG_DT is null
		  and MSG_DCD = '${msg_type}'
		  and GROUP_NO is NULL
		  and MMS_IMG_NO is not null
	</select>

	<update id="req_mms_group_update" parameterType = "com.dhn.client.bean.SQLParameter" >
		update ${msg_table}
		set MSG_SNDNG_STTS_CD = '1',
			GROUP_NO = '${group_no}'
		where DAT_ID in (
			select DAT_ID
			from ${msg_table}
			where MSG_SNDNG_STTS_CD = '0'
			  and NVL(RSVT_DT,SYSDATE) &lt;= SYSDATE
		  and SNDNG_DT is null
		  and MSG_DCD = '${msg_type}'
		  and GROUP_NO is null
		  and MMS_IMG_NO is not null
			fetch first 1000 rows only
			)
	</update>

	<select id="req_mms_image" parameterType = "com.dhn.client.bean.SQLParameter" resultType = "com.dhn.client.bean.MMSImageBean">
		select LTR_ATCH_FILE_PATH_ADDR as file1
			 ,LTR_ATCH_FILE_PATH_ADDR2 as file2
			 ,LTR_ATCH_FILE_PATH_ADDR3 as file3
		from ${msg_table}
		where MSG_SNDNG_STTS_CD = '0'
		  and NVL(RSVT_DT,SYSDATE) &lt;= SYSDATE
		  and SNDNG_DT is null
		  and MSG_DCD = '${msg_type}'
		  and GROUP_NO is null
		  and LTR_ATCH_FILE_PATH_ADDR is not null
		group by LTR_ATCH_FILE_PATH_ADDR, LTR_ATCH_FILE_PATH_ADDR2, LTR_ATCH_FILE_PATH_ADDR3
	</select>

	<update id="req_mms_key_update" parameterType = "com.dhn.client.bean.SQLParameter" >
		update ${msg_table}
		set MMS_IMG_NO = '${mms_key}'
		where MSG_SNDNG_STTS_CD = '0'
		  and GROUP_NO is null
		  and MSG_DCD = '${msg_type}'
		  and NVL(RSVT_DT,SYSDATE) &lt;= SYSDATE
		  and NVL(LTR_ATCH_FILE_PATH_ADDR,'X') = '${file1}'
		  and NVL(LTR_ATCH_FILE_PATH_ADDR2,'X') = '${file2}'
		  and NVL(LTR_ATCH_FILE_PATH_ADDR3,'X') = '${file3}'
	</update>

	<resultMap id="RequestTable" type="com.dhn.client.bean.RequestBean">
		<result column="DATA_ID" property="msgid"/>
		<result column="message_type" property="messagetype"/>
		<result column="msg_txt" property="msg"/>
		<result column="sms_txt" property="msgsms"/>
		<result column="call_to" property="phn"/>
		<result column="call_from" property="smssender"/>
		<result column="pcom" property="pcom"/>
		<result column="mms_img_key" property="pinvoice"/>
		<result column="smskind" property="smskind"/>
		<result column="msg_txt_title" property="smslmstit"/>
		<result column="crypto" property="crypto"/>
		<result column="curr_date" property="regdt"/>
		<result column="snd_dttm" property="reservedt"/>
	</resultMap>

	<select id="req_sms_select" parameterType = "com.dhn.client.bean.SQLParameter" resultMap = "RequestTable">
		select
			trim(DAT_ID) as DATA_ID,
			'PH' as message_type,
			LTR_MSG_CN as msg_txt,
			LTR_MSG_CN as sms_txt,
			trim(RCVR_TELNO) as call_to,
			trim(TRNSPS_TELNO) as call_from,
			'' as msg_txt_title,
			CASE
				WHEN LENGTHB(LTR_MSG_CN) > 90 THEN 'L'
				ELSE 'S'
				END AS smskind,
			'D' as pcom,
			TO_CHAR(SYSDATE, 'yyyy-mm-dd hh24:mi:ss') as curr_date,
			NVL(TO_CHAR(RSVT_DT, 'yyyymmddhh24miss'), '00000000000000') as snd_dttm
		from ${msg_table}
		where MSG_SNDNG_STTS_CD = '1'
		  and GROUP_NO = '${group_no}'
		  and MSG_DCD = '${msg_type}'
	</select>

	<select id="req_lms_select" parameterType = "com.dhn.client.bean.SQLParameter" resultMap = "RequestTable">
		select
			trim(DAT_ID) as DATA_ID,
			'PH' as message_type,
			LTR_MSG_CN as msg_txt,
			LTR_MSG_CN as sms_txt,
			trim(RCVR_TELNO) as call_to,
			trim(TRNSPS_TELNO) as call_from,
			LTR_MSG_TTL_NM as msg_txt_title,
			'L' as smskind,
			'D' as pcom,
			TO_CHAR(SYSDATE, 'yyyy-mm-dd hh24:mi:ss') as curr_date,
			NVL(TO_CHAR(RSVT_DT, 'yyyymmddhh24miss'), '00000000000000') as snd_dttm
		from ${msg_table}
		where MSG_SNDNG_STTS_CD = '1'
		  and GROUP_NO = '${group_no}'
		  and MSG_DCD = '${msg_type}'
	</select>

	<select id="req_mms_select" parameterType = "com.dhn.client.bean.SQLParameter" resultMap = "RequestTable">
		select
			trim(DAT_ID) as DATA_ID,
			'PH' as message_type,
			LTR_MSG_CN as msg_txt,
			LTR_MSG_CN as sms_txt,
			trim(RCVR_TELNO) as call_to,
			trim(TRNSPS_TELNO) as call_from,
			LTR_MSG_TTL_NM as msg_txt_title,
			'M' as smskind,
			'D' as pcom,
			TO_CHAR(SYSDATE, 'yyyy-mm-dd hh24:mi:ss') as curr_date,
			NVL(TO_CHAR(RSVT_DT, 'yyyymmddhh24miss'), '00000000000000') as snd_dttm,
			MMS_IMG_NO as mms_img_key
		from ${msg_table}
		where MSG_SNDNG_STTS_CD = '1'
		  and GROUP_NO = '${group_no}'
		  and MSG_DCD = '${msg_type}'
		  and MMS_IMG_NO is not null
	</select>

	<update id="req_sent_complete" parameterType = "com.dhn.client.bean.SQLParameter" >
		update ${msg_table}
		set MSG_SNDNG_STTS_CD = '5',
			SNDNG_DT = SYSDATE
		where
			MSG_SNDNG_STTS_CD = '1'
		  and GROUP_NO = '${group_no}'
		  and MSG_DCD = '${msg_type}'
	</update>

	<update id="req_sent_init" parameterType = "com.dhn.client.bean.SQLParameter" >

		update ${msg_table}
		set MSG_SNDNG_STTS_CD = '0',
			GROUP_NO = NULL,
			SNDNG_DT = NULL
		where
			MSG_SNDNG_STTS_CD = '1'
		  and GROUP_NO = '${group_no}'
		  and MSG_DCD = '${msg_type}'
	</update>

	<update id="result_log_insert1" parameterType="com.dhn.client.bean.Msg_Log">
		update ${msg_table}
		set MSG_SNDNG_STTS_CD = '${status}',
			MSG_SNDNG_ERR_CD = '${msg_err_code}',
			SNDNG_CMPTN_DT = TO_TIMESTAMP('${sndg_cpee_dt}', 'YYYY-MM-DD HH24:MI:SS'),
			RSND_MSG_DCD = '${agan_sms_type}',
			MSG_SNDNG_TLCMCO_CD = '${agan_tel_info}'
		where DAT_ID = '${msgid}'
	</update>

	<insert id="result_log_insert2" parameterType="com.dhn.client.bean.Msg_Log">
		insert into ${log_table} select * from ${msg_table} where DAT_ID = '${msgid}'
	</insert>

	<delete id="result_log_insert3" parameterType="com.dhn.client.bean.Msg_Log">
		delete from ${msg_table} where DAT_ID = '${msgid}'
	</delete>

	<update id="mms_fail_insert1" parameterType="com.dhn.client.bean.Msg_Log">
		update ${msg_table}
		set MSG_SNDNG_STTS_CD = '4',
			MSG_SNDNG_ERR_CD = 'x',
			SNDNG_CMPTN_DT = SYSDATE,
			SNDNG_DT = SYSDATE,
			RSND_MSG_DCD = '4',
			MSG_SNDNG_TLCMCO_CD = '4'
		where MSG_SNDNG_STTS_CD = '0'
		  and GROUP_NO is null
		  and MSG_DCD = '${msg_type}'
		  and NVL(RSVT_DT,SYSDATE) &lt;= SYSDATE
		  and NVL(LTR_ATCH_FILE_PATH_ADDR,'X') = '${file1}'
		  and NVL(LTR_ATCH_FILE_PATH_ADDR2,'X') = '${file2}'
		  and NVL(LTR_ATCH_FILE_PATH_ADDR3,'X') = '${file3}'
	</update>

	<insert id="mms_fail_insert2" parameterType="com.dhn.client.bean.Msg_Log">
		insert into ${log_table}
		select * from ${msg_table}
		 where MSG_SNDNG_STTS_CD = '4'
		   and GROUP_NO is null
		   and MSG_DCD = '${msg_type}'
		   and NVL(RSVT_DT,SYSDATE) &lt;= SYSDATE
		   and NVL(LTR_ATCH_FILE_PATH_ADDR,'X') = '${file1}'
		   and NVL(LTR_ATCH_FILE_PATH_ADDR2,'X') = '${file2}'
		   and NVL(LTR_ATCH_FILE_PATH_ADDR3,'X') = '${file3}'
	</insert>

	<delete id="mms_fail_insert3" parameterType="com.dhn.client.bean.Msg_Log">
		delete from ${msg_table}
	    where MSG_SNDNG_STTS_CD = '4'
		  and GROUP_NO is null
		  and MSG_DCD = '${msg_type}'
		  and NVL(RSVT_DT,SYSDATE) &lt;= SYSDATE
		  and NVL(LTR_ATCH_FILE_PATH_ADDR,'X') = '${file1}'
		  and NVL(LTR_ATCH_FILE_PATH_ADDR2,'X') = '${file2}'
		  and NVL(LTR_ATCH_FILE_PATH_ADDR3,'X') = '${file3}'
	</delete>

</mapper>  